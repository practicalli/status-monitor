(ns practicalli.status-monitor-service-test
  (:require [clojure.test :refer :all]
            [practicalli.status-monitor-service :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
